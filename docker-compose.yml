version: '3.8'

services:
  smartpark:
    build: .
    container_name: smartpark-app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
