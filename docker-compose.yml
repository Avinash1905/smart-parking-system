version: '3.8'

services:
  smartpark-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartpark-server
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
